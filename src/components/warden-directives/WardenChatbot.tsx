/**
 * General Warden chatbot retired by A1.
 * The component remains as a zero-render compatibility export so layout imports
 * do not break while routes migrate away from the former floating AI surface.
 */
export function WardenChatbot() {
	return null;
}
