# Placeholder Report

Generated: `2026-01-08T15:23:24.436Z`

This report is generated by `node scripts/placeholder-scan.js`.

Summary:

- **P0**: 0
- **P1**: 0
- **P2**: 36

---

## Findings

| Severity | File | Lines | What | Replacement plan | Tests needed |
|---:|---|---:|---|---|---|
| **P2** | `scripts/compendium/validate.js` | 32 | throw new Error: Invalid JSON: ${filePath} | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `scripts/import-content.js` | 24-518 | throw new Error (x7): --dir requires a value | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/components/campaign/CampaignSettings.tsx` | 41-74 | throw new Error (x4): You must be logged in to update campaigns | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/components/ui/carousel.tsx` | 35 | throw new Error: useCarousel must be used within a <Carousel /> | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/components/ui/chart.tsx` | 26 | throw new Error: useChart must be used within a <ChartContainer /> | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/components/ui/form.tsx` | 41 | throw new Error: useFormField should be used within <FormField> | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/components/ui/sidebar.tsx` | 37 | throw new Error: useSidebar must be used within a SidebarProvider. | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/hooks/useCampaignCharacters.ts` | 57 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCampaignChat.ts` | 102 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCampaignNotes.ts` | 56 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCampaigns.ts` | 141-222 | throw new Error (x3): Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCharacterBackup.ts` | 114 | throw new Error: Invalid backup file format | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCharacters.ts` | 221-305 | throw new Error (x4): Hunter not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useCharacterTemplates.ts` | 123-255 | throw new Error (x3): Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useProfile.ts` | 48 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useRollHistory.ts` | 41 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useRunes.ts` | 131-272 | throw new Error (x7): Sign in required to inscribe runes | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useSavedSovereigns.ts` | 79 | throw new Error: Must be logged in to save Sovereigns | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useShadowSoldiers.ts` | 107-219 | throw new Error (x2): Soldier not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useSpellSlots.ts` | 131 | throw new Error: Hunter not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/hooks/useUserFavorites.ts` | 67 | throw new Error: Not authenticated | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/integrations/supabase/client.ts` | 41 | throw new Error: Global fetch is not available | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add tests appropriate to the affected subsystem (unit for logic, e2e for user flows). |
| **P2** | `src/lib/automation.ts` | 31-88 | throw new Error (x2): Character not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/bulkOperations.ts` | 102-103 | TODO, throw new Error: Bulk equipment add not supported - equipment must be adde... | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/bulkOperations.ts` | 133-178 | throw new Error (x2): Character not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/compendiumResolver.ts` | 66-150 | throw new Error (x2): Unknown entry type: ${type} | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/contentImporter.ts` | 510 | throw new Error: Failed to parse YAML content: ${error instanceof Error ? ... | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/diceRoller.ts` | 20 | throw new Error: Invalid dice string: ${diceStr} | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/envValidation.ts` | 70-88 | throw new Error (x2): errorMessage | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/export.ts` | 34-125 | throw new Error (x2): Character not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/guestStore.ts` | 257-573 | throw new Error (x10): Local character not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/restSystem.ts` | 29-108 | throw new Error (x2): Hunter not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/lib/rollEngine.ts` | 28 | throw new Error: Invalid dice formula: ${formula} | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend unit tests (Vitest) for the affected module. |
| **P2** | `src/pages/CharacterNew.tsx` | 212 | throw new Error: Job not found | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/pages/Compendium.tsx` | 190 | throw new Error: Supabase configuration is missing. Please set VITE_SUPABA... | Audit this throw: if it is reachable via normal user flows, replace with user-safe handling (toasts/UI error state) and keep throws only for truly impossible states. Add tests for both success and failure paths. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |
| **P2** | `src/pages/dm-tools/InitiativeTracker.test.tsx` | 60 | throw new Error: Round container not found | If this is an intentional test sentinel, replace placeholder wording with an explicit assertion message; otherwise, implement the missing behavior and expand test coverage. | Add/extend integration/e2e tests (Playwright) covering the affected flow. |

---

## Notes

- This scan is conservative and may include some intentional sentinel strings (especially in docs/tests).
- The goal for release is **zero placeholder markers** in production code paths.
